# CurveScope (web)

Next.js UI for **[CurveScope](../README.md)** — the TESS light-curve anomaly lab.

Product copy lives in [`lib/brand.ts`](lib/brand.ts). Run the app from the repo root with `npm run dev` (see root `README.md`) or here with `npm run dev` after the API is up on port 8000.
