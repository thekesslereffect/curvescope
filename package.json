{
  "name": "curvescope",
  "private": true,
  "description": "CurveScope — TESS light-curve anomaly lab (run backend + frontend together)",
  "scripts": {
    "predev": "node scripts/kill-stale.js",
    "dev": "concurrently -k -n backend,frontend -c blue,magenta \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && .\\venv\\Scripts\\python.exe -m uvicorn main:app --reload --host 127.0.0.1 --port 8000",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
